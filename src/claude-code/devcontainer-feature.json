{
    "name": "Claude Code CLI",
    "id": "claude-code",
    "version": "1.0.0",
    "description": "Installs the Claude Code CLI using the native installer (no Node.js required)",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "Version of Claude Code to install. Use 'latest', 'stable', or a semver like '1.0.58'."
        }
    },
    "containerEnv": {
        "CLAUDE_CODE_SKIP_UPDATE_CHECK": "true"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "anthropic.claude-code"
            ]
        }
    },
    "installsAfter": []
}
